class Explosion {

  constructor() {
    this.pos = createVector(width / 2, height / 2);
    this.vel = createVector();
    this.size = 3;
    this.r = 30;
  }

  draw() {

    // Random number of points 6-12
    // Random offset for points

  }
}
